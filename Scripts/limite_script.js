// Dados da restrição
const dados_materias = [
    nome,
]

const dados_limitacao_card = [
    id_card,
    quantidade,
    quantidade_max,
    quantidade_min
]

const dados_limitacao_material = [
    id_material,
    quantidade
]

// Não sei como programar a interface desta parte, então vai o que pensei:

// LIMITAÇÃO CARD
// A pessoa escolhe o Card, com base no ID e escreva a quantidade maxima e minima

// LIMITAÇÃO MATERIAL
/* 
Ao criar um card, armazene o nome do material em uma lista para ser usado nas restrições,
exibida em uma listbox pro usuário escolher qual material quer limitar, escolhendo a 
quantidade máxima
*/  

// COMO FUNCIONARIA O CALCULO DE MAX COM RESTRIÇÃO
/*

Vamos por partes

1 - MINIMO

if(cards.dados_limtacao_card.quantidade_min ! NULL)
{
    cards.dados_limtacao_card.quantidade += cards.dados_limtacao_card.quantidade_min
}

*/
